/**
 * Variáveis de controle interno
 */

var varStateIndex = 0 //importante para o databinding, controla actualização das regiões da página em que a variável é chamada
var var_state_list = [] //lista de variáveis de estados já criadas